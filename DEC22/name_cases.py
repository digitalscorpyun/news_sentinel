name = "Frederick"
message= "Hello " + name + " how are you?"
print(message)
# The name of a person is stored in a variable called name. The variable name is then used in a message that is printed to the screen.